{
  "name": "hackalem-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dashboard.mjs && node --check recommendations.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
